# Firebase Functions
firebase-functions==0.5.0
firebase-admin==6.9.0

# Swiss Ephemeris - Precision Astrology Calculations
pyswisseph==2.10.3.2

# Neo4j Graph Database
neo4j==5.28.3

# Numerical / AI
numpy==1.26.4
pandas==2.3.3
anthropic==0.79.0            # Claude API for biographic extraction

# Chinese Calendar & BaZi Engine (Joey Yap Standard)
sxtwl==1.1.0       # 寿星万年历 - Accurate solar terms, Lichun timing
lunardate==0.2.2    # Lunar calendar fallback

# Utilities
python-dateutil==2.9.0
pytz==2025.2

# Biography Ingestion Pipeline (Hello History RAG)
PyMuPDF==1.26.7              # PDF reading (fitz)
pdfplumber==0.11.9           # PDF reading fallback
openai==1.104.1              # Embeddings and enrichment
psycopg2-binary==2.9.11      # PostgreSQL connection (local dev)
pgvector==0.4.2              # Vector storage in PostgreSQL

# ePub Ingestion (Reagan Diaries)
EbookLib==0.20               # ePub parsing
beautifulsoup4==4.14.3       # HTML extraction from ePub
lxml==5.4.0                  # XML/HTML parser (bs4 backend)
langchain-text-splitters==0.3.11  # 20% overlap chunking for long entries

# Cloud SQL Connection (Cloud Functions)
pg8000==1.31.5               # Pure Python PostgreSQL driver (required for Cloud SQL)
SQLAlchemy==2.0.46            # Database ORM
cloud-sql-python-connector==1.20.0  # Google Cloud SQL connector
